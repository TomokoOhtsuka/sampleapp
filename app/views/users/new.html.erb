<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |foober| %>
      <%= foober.label :name %>
      <%= foober.text_field :name %>

      <%= foober.label :email %>
      <%= foober.email_field :email %>

      <%= foober.label :password %>
      <%= foober.password_field :password %>

      <%= foober.label :password_confirmation, "Confirmation" %>
      <%= foober.password_field :password_confirmation %>

      <%= foober.submit "Create my account", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>